{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "ExtensionServiceDeployInput",
  "type": "object",
  "required": ["GITHUB_REPOSITORY", "ENVIRONMENT", "VARS"],
  "properties": {
    "GITHUB_REPOSITORY": { "type": "string" },
    "ENVIRONMENT": { "type": "string" },
    "VARS": {
      "type": "object",
      "additionalProperties": {
        "type": ["string", "number", "boolean"]
      }
    },
    "SECRETS": {
      "type": "object",
      "additionalProperties": {
        "type": "string"
      }
    },
    "lambda_config": { "type": "object", "description": "Legacy format only" },
    "ecs_environment": {
      "type": "object",
      "description": "Legacy format only",
      "additionalProperties": {
        "type": ["string", "number", "boolean"]
      }
    },
    "ecr_image_uri": { "type": "string", "description": "Legacy format only" }
  }
}
